<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:descendantFocusability="blocksDescendants" >

    <TextView
        android:id="@+id/listItemIdTxt"
        android:layout_width="20dp"
        android:layout_height="30dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="20dp"
        android:inputType="number"
        android:textColor="@color/white" />

    <TextView
        android:id="@+id/listItemNameTxt"
        android:layout_width="150dp"
        android:layout_height="30dp"
        android:layout_toRightOf="@id/listItemIdTxt"
        android:singleLine="false"
        android:layout_marginTop="20dp" />

    <TextView
        android:id="@+id/listItemQtyTxt"
        android:layout_width="80dp"
        android:layout_height="30dp"
        android:layout_toRightOf="@id/listItemNameTxt"
        android:layout_marginTop="20dp"
        android:inputType="number" />

    <TextView
        android:id="@+id/listItemUnitPriceTxt"
        android:layout_width="80dp"
        android:layout_height="30dp"
        android:layout_toRightOf="@id/listItemQtyTxt"
        android:layout_marginTop="20dp"
        android:inputType="numberDecimal" />

</RelativeLayout>